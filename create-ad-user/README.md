A simple script that creates a new Active Directory user based on parameters passed in. It also generates a random password using the numbers 0-9 and uppercase and lowercase letters.

The user is created in the domain LAB.local, which must be changed within the script if a the user should be created in a different domain.